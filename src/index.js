import React from "react";
import ReactDOM from "react-dom/client";

import App from "./App";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("root")); //This is the only div in pyblic/index.html

root.render(<App />); //This is the only div in pyblic/index.html
